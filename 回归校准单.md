# 回归校准单 · Wafer_annotation_web

> 用法:每轮测试开工前通读;每条问题一行"症状 → 最小复现探针 → 兜住的规则"。
> 已修的条目在 pytest(`tests/test_api.py`)有同名回归钉,修复合入后即为最快冒烟单。
> 建档:2026-09-17 find-bug-skill 独立审查轮。

| # | 症状 | 最小复现探针 | 兜住的规则 | 状态 |
|---|---|---|---|---|
| 1 | 首访页面浏览器 console 刷 401 error | 无 token GET `/api/me` | 必须返 200 `{"name":null}` | ✅已修(test_me_anonymous_returns_null_not_401) |
| 2 | 点"退出"后刷新仍登录 | 登录 → POST `/api/logout` → 带 cookie GET `/api/queue` | 必须 401(退出必须清 cookie) | ✅已修(test_logout_clears_cookie) |
| 3 | 预分配出现"自己标自己"(概率≈1/4,建库时静默发生) | `SELECT COUNT(*) FROM images WHERE assignee_a=assignee_b` | 必须 0;配对用堆式"剩余最多者优先" | ✅已修(test_assignment + 建库后人工核) |
| 4 | 定稿后作者改判,csv 变 pending 而 xml 仍导旧框、前端仍显示已定稿(三口径矛盾) | 一致定稿 → 作者 submit 不同框 → 对比 `images.final_id` 指向 / labels_voted 行 / voc_xml 包 | final 必须指向 revoked=0 的标注;三导出同口径 | ⚠️未修(报告 P1-1,修后补回归钉) |
| 5 | 3 票 1:1:1 平票瞬间按"首得票组"定稿(文档说 4 票兜底、取最早提交) | 三组分裂 → 三人各投各的 → 看第 3 票响应 | ≥3 票且最高组唯一才定;并列到 4 票才兜底且取最早提交组 | ⚠️未修(报告 P1-2) |
| 6 | 旧票指向已撤销候选仍计入"≥3 票"门槛;全悬空时 try_settle 可 500 | 票决后涉事人 revoke/改判全部候选 → 新提交触发 try_settle | 票数只数有效候选的票;tally 空按无票处理 | ⚠️未修(报告 P1-3,与 #4 同处收口) |
| 7 | 盲审面板恶意理由注入脚本 | dispute 理由填 `<img src=x onerror=...>` → 打开面板断言 `window.__xss!==1` | 一切用户输入渲染必须 esc() | ✅已验证(UI 探针,本轮通过) |
| 8 | 未登录可访问保护接口 | 15 条 GET/POST 无 token 直打 | 全 401 | ✅已验证(test_unauthorized_matrix) |
| 9 | 空图勾选与框冲突语义 | submit is_empty=true 且带框 | 空图优先,框必须被丢弃 | ✅已验证(test_box_validation_matrix) |
| 10 | 同人同轮重复投票累计多票 | 同人连投两票 → review votes 计数 | upsert,计数不变、my_vote 为最后改票 | ✅已验证(test_vote_upsert_same_round) |
