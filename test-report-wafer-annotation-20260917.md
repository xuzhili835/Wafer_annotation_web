# 测试报告 · 光伏标注站独立审查轮

> 姿态:**只测不修**(审查模式)。时间:2026-09-17 凌晨。测试对象:产品代码 commit `5b0a83f`
> (Wafer_annotation_web main);本轮新增仅部署工作流加固、pytest 回归用例与本文档,未动产品逻辑。
> 方法:find-bug-skill(业务闭环断言 + 对抗探针 + API/DB/UI 三层交叉验证)。

## 覆盖声明(最前)

| 层 | 状态 | 说明 |
|---|---|---|
| API 层 | ✅ 已测 | httpx 直打 127.0.0.1:8100(绕系统代理),18 组探针 + pytest 20 例 |
| DB 层 | ✅ 已测 | SQLite 直查对账(行数/自配对/final_id 指向/票行),基线前后对账一致 |
| UI 层 | ✅ 已测 | 有头 Edge:盲审面板 XSS 渲染、登录 Enter、五视图截图走查、投票/定稿/真名解锁闭环 |

**未测项**:并发竞争(4 人同时提交同图的 SQLite 写竞态,单进程 uvicorn 下风险低,未构造);
构建/多浏览器兼容(仅 Edge);真实弱网;文件上传(本产品无此面);封板后的持续写入(行为已探明,
是否算问题待拍板,见观察项 6)。
**默认出界的质量维度**:性能效率(未测,2核2G 下的响应压测未做)/ 兼容性(未测,仅 Chromium 系)/
无障碍(未测)——三项本轮均未显式纳入,残余风险如实声明。

## 一、发现的问题(严重度排序)

### P1-1【中危】定稿后作者改判 → "幽灵定稿",三种口径互相矛盾
- **现象(普通人版)**:一张图两人标得一致、已定稿之后,其中一人反悔改了一版重新提交。
  这时进度页仍显示"已定稿",定稿 CSV 里这张图却变成"未定稿",而 XML 包里还在用他**改判前**的旧框。
  训练的人拿到两份互相矛盾的标签文件。
- **规则**:latest-wins 改判后,图片的定稿状态应基于最新有效标注重新裁决;三导出必须同一口径。
- **实际**:`/api/submit` 撤旧份时从不清 `images.final_id`;`try_settle` 对已定稿图直接早退返回旧值。
  实测:定稿 id=31 被作者改判后,`final_id` 仍=31(已 revoked=1);labels_voted.csv 该行变
  `pending`;voc_xml.zip 仍含旧框;review 接口 `final=true, revealed=true`(真名照常解锁)。
- **复现**:一致定稿 → 作者 POST /api/submit 不同框 → 查 images.final_id、labels_voted.csv、
  voc_xml.zip 三处(完整脚本见 .shots/probes.py P19 段)。
- **根因定位**:`app/main.py` submit()(不 unset final)+ try_settle() 首行早退。
- **严重度**:中。触发路径是产品明示支持的"回看改判",一旦发生,训练数据口径静默错乱;但仅在
  "定稿后改判"时出现,定稿前改判不受影响。

### P1-2【低危】票决兜底与文档不符:3 票并列即兜底定稿,且取的不是"最早提交"
- **现象(普通人版)**:三张票各投一家的最僵局面,本想等第四人,现在第三票落地瞬间就按
  "第一得票组"定了稿;文档写的是"4 票并列才兜底、取最早提交"。
- **规则**(docstring 与启动文档口径):≥3 票且最高组唯一才定;4 票后仍并列 → 兜底取最早提交的组。
- **实际**:else 分支无条件执行 `groups[tops[0]]`;`tops[0]` 取自 tally dict 迭代序(首个被投组),
  非最小标注 id 组。实测 3 票 1:1:1 即定稿。
- **复现**:三组分裂 → 三人各投各的 → 第三票响应即含 final_id。
- **根因定位**:`app/main.py` try_settle() 行 194-197(if/else 两支结尾相同,兜底条件失效)。
- **严重度**:低。触发需精确平票,影响是"提前定稿 + 取组可能与文档承诺不一致"。

### P1-3【低危】悬空票计入"≥3 票"门槛,且存在理论 500 路径
- **现象(普通人版)**:投票后大家改主意重新标注,旧候选作废了,但那些旧票还在账上凑数,
  可能提前触发定稿;极端情况下(旧票全部作废后又凑够 3 张)后端会算不明白直接报错。
- **规则**:票数应只数"仍指向有效候选"的票;无有效票时按无票处理。
- **实际**:`len(votes)` 不过滤 revoked 候选;`tally` 为空时 `tops=[]` 走 `groups[tops[0]]`
  → IndexError → HTTP 500(代码走读确认;实测两次改判探针因早期分支未到达该行,未复现崩溃,
  但通过 revoke 端点连续撤销可构造,见校准单)。
- **根因定位**:`app/main.py` try_settle() 票决段。
- **严重度**:低(触发链条长),建议与 P1-1 同一处一并收口。

## 二、观察项 / 设计确认(不算 bug,供拍板)

1. **封板后不锁定写入**:3/4 封板后 submit/vote/dispute 照常。若"封板=冻结",需在三个写入口加闸;
   若"封板=导出语义",现状即可——请拍板。
2. **cookie 未加 secure 标志**:本地 http 无碍;上线走 CF https 后建议 `secure=True`(需与本地
   http 调试权衡)。
3. **登录无速率限制**:4 人内部工具可接受;令牌是唯一门槛,泄露即全权。
4. **progress 人员表硬编码** `("cmx","hce","zj","zzq")`,与 config.MEMBERS 重复,换人时两处要改。
5. **IoU 聚类不满足传递性**(A≈B、B≈C 推不出 A≈C),分组结果依赖提交顺序;病态案例才触发。
6. **dispute 可对无标注图发起**(round+1 而已,无实害)。
7. 导出 CSV 无 BOM:表头全英文,现状 Excel 打开无乱码,不必改;若未来表头中文化需转 utf-8-sig。

## 三、已验证正确的逻辑(通过项,下轮回归对照)

- **鉴权**:15 条保护路由未登录全 401;坏 token 401;cookie/X-Token 双通道一致;退出后旧 cookie
  失效(本轮已修复并钉住);首访 /api/me 返 200 {name:null}(已修复并钉住)。
- **分配**:510 图每张恰好 2 名不同标注人、4 人各 255、自配对 0(堆式配对修复后稳定,pytest×6)。
- **校验矩阵 10 项**:未知码 400 / <2px 400 / 倒序坐标规范化 / 越界夹紧 / 勾空图丢框 / 无框 400 /
  不存在图 404 / 路径穿越 404 / 坏 JSON 422 / 坏类型 422,全部符合预期。
- **定稿引擎主干**:两份一致自动定稿;分歧进盲审;第三人事实多数;3 票 2:1 定稿;定稿前候选全匿名
  (甲乙丙丁)、定稿后真名解锁;异议重开轮次+1、旧票自然作废、第 2 轮需重新满 3 票。
- **投票**:同人同轮改票 upsert 不累计;投已撤销候选 404;封板同人重复幂等、3/4 判定正确。
- **导出**:留痕 csv 含 revoked 行(总行数=库内总数);labels_voted 逐图一行含 pending 标记;
  XML 仅非空定稿图入包、结构与测试集同构(annotation/size/object/bndbox)、文件名带时间戳、
  X-Final-Count 准确。
- **安全**:盲审面板恶意理由(XSS 探针 `<img onerror>`)被转义为纯文本,脚本未执行;SQL 全参数化
  (注入式 stem 走 404 不 500)。
- **UI**:五视图截图走查(Nielsen 十条)无阻塞问题;登录框 Enter 可提交;画布拖框/改判/热力跳转
  前轮已实测。

## 四、数据还原说明

- 探针产生的标注/票/异议/封板已全部清除:删除 labels.db(-wal/-shm)后重启重建;
  基线对账 tokens=4 / images=510 / 其余 0 / 自配对=0,与开测快照一行不差。
- 服务以最新代码(含本轮零产品改动)在 127.0.0.1:8100 保持运行,干净空库。
- pytest 临时产物在 `C:\Users\legion\AppData\Local\Temp\pytest-of-legion\`(每轮 <1MB,C 盘间接
  占用,可随时整目录删除);探针脚本在 `D:\Desktop\Wafer_annotation_web\.shots\`(已 gitignore)。

---

## 修复记录(2026-09-17 上午,经用户授权进入修复模式)

- **P1-1 已修**:`submit` 在 latest-wins 撤旧份时,若旧份正是当前 final → 先摘牌再重裁;
  `try_settle` 开头增加自愈(final 指向已撤销标注时摘牌)。回归钉:
  `test_resubmit_conflict_after_final_no_ghost`(冲突路径)、
  `test_resubmit_final_author_still_agree_refinal`(仍一致路径)。
- **P1-2 已修**:票决重写——≥3 票且最高组唯一(或过半)才定;满 4 票仍并列才兜底,
  兜底取并列各组中最早提交的一份;3 票并列继续等票。回归钉:`test_tie_of_three_waits_for_4th_vote`。
- **P1-3 已修**:try_settle 与 review 的票数统一只数"仍指向有效候选"的票,悬空票不计,
  IndexError 路径消除。回归钉:`test_dangling_votes_excluded_from_count`。
- **更正一处审查结论**:原「已验证正确」一节写"XML 仅非空定稿图入包"为**空转断言**(当轮无空图
  定稿,未真正覆盖)。实际规格:`export.py` 对所有定稿图出 XML,空图定稿导出 0 object 的
  annotation(即负样本),与 labels_voted.csv 的 `empty` 标记口径一致。已在
  `test_voc_xml_matches_final_set` 中钉死该语义;训练侧 `wafer.prepare` 接入时需容忍 0-object XML。
- 修复后回归:pytest 24/24(本地 Windows ×6 连跑)+ GitHub Actions Ubuntu 全绿;18 项 API 探针
  全绿;幽灵定稿原探针复跑确认三口径一致。
